# GaussianUpdate: 持续3D高斯泼溅更新方法详解

> **论文信息**: *GaussianUpdate: Continual 3D Gaussian Splatting Update for Changing Environments*
> **发表会议**: ICCV 2025
> **作者**: Lin Zeng, Boming Zhao, Jiarui Hu, Xujie Shen, Ziqiang Dang, Hujun Bao, Zhaopeng Cui
> **机构**: 浙江大学 CAD&CG 国家重点实验室

---

## 一、任务概述

### 1.1 问题定义

给定一个已训练的3D高斯场（3D Gaussian Field），以及随时间变化的场景图像和对应相机位姿，GaussianUpdate 能够**持续更新**该场景的3D高斯表示，使其能够：

1. 在任意历史时刻渲染高质量的新视角图像
2. 可视化不同时刻之间的场景几何变化
3. 避免灾难性遗忘（Catastrophic Forgetting）

### 1.2 持续学习任务形式化

在3D高斯场中，持续学习任务定义为：

1. **新数据到达**：在新的时间步 $t$，收集一组多视角图像 $I^{gt}_{f} \ (f=1...N)$ 及对应相机位姿
2. **模型更新**：基于最新观测，在保留先验场景知识的前提下，从 $t-1$ 时刻的高斯场获得 $t$ 时刻的更新高斯场
3. **持续构建**：构建一个灵活的、持续的3D高斯场，实现照片级新视角合成，并支持时变场景变化可视化

### 1.3 场景变化的三种类型

GaussianUpdate 将场景变化显式建模为三种类型：

| 变化类型 | 描述 | 处理策略 |
|---------|------|---------|
| **光照变化** | 全局照明变化（如白天/夜晚） | 全局外观模型（4D Hash + Tiny MLP） |
| **物体消失** | 原有物体不再存在 | 可学习移除因子 + DBSCAN聚类剪枝 |
| **物体新增** | 新物体出现在场景中 | COLMAP稀疏点初始化 + densify操作 |

---

## 二、基础表示：3D高斯泼溅

### 2.1 3D高斯基元定义

一个3D场景被定义为3D高斯基元的集合：

$$\mathcal{G} = \{G_i : (\mu_i, \Sigma_i, \sigma_i, Y_i) \mid i = 1, ..., N\}$$

其中：
- $\mu_i \in \mathbb{R}^3$：高斯中心位置（3D坐标）
- $\Sigma_i \in \mathbb{R}^{3\times3}$：3D协方差矩阵
- $\sigma_i$：不透明度（opacity）
- $Y_i$：球谐系数（spherical harmonics coefficients）

### 2.2 协方差矩阵分解

为确保正半定性和梯度下降的稳定性，$\Sigma$ 通过缩放矩阵 $S = \text{diag}([s])$ 和旋转矩阵 $R$ 参数化：

$$\Sigma = RSS^TR^T$$

### 2.3 2D投影与渲染

3D高斯通过世界到相机的投影变换 $W$ 和雅可比矩阵 $J$ 投影到2D：

$$\Sigma' = JW\Sigma W^TJ^T$$

渲染采用前到后的 $\alpha$-blending：

$$\alpha_i = \sigma\left(\frac{1}{2}e^{-(x-\mu')^T\Sigma'^{-1}(x-\mu')}\right)$$

$$T_i = \prod_{j=1}^{i-1}(1 - \alpha_j)$$

$$\mathcal{C} = \sum_{i \in \mathcal{N}} c_i \alpha_i T_i$$

其中 $c_i$ 是从球谐系数获得的颜色，$T_i$ 是累积透射率。

---

## 三、核心方法：三阶段更新策略

GaussianUpdate 的核心是一个**渐进式三阶段更新方案**，将光照和布局变化解耦，避免 $\alpha$-blending 中外观和几何属性的紧耦合导致的歧义和过拟合问题。

### 3.1 系统总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    GaussianUpdate 系统架构                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  输入：已有3D高斯场 + 当前时刻多视角图像 + 相机位姿              │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Visibility Pool（可见性池）                           │   │
│  │  - 记录所有时刻涉及的3D高斯基元                         │   │
│  │  - 被剪枝的高斯设为不活跃，反之设为活跃                   │   │
│  │  - 存储历史训练视角的相机外参                            │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  4D Hash Grid（全局外观模型）                          │   │
│  │  - L=16层哈希表                                       │   │
│  │  - 每层特征维度 F=4                                   │   │
│  │  - 最粗网格分辨率：16                                  │   │
│  │  - 输入：位置 xyz ∈ R³ + 时间戳 t ∈ R¹               │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Tiny MLP（外观解码器）                                │   │
│  │  - 2层全连接网络                                       │   │
│  │  - 每层128个神经元                                     │   │
│  │  - 输出：51维特征（48维球谐系数 + 3维缩放）              │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ 第一阶段  │→│ 第二阶段  │→│ 第三阶段  │                  │
│  │ 全局外观  │  │ 几何布局  │  │ 联合精炼  │                  │
│  │ 更新     │  │ 更新     │  │          │                  │
│  │ 7K迭代   │  │ 8K迭代   │  │ 15K迭代  │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
│                                                             │
│  输出：更新后的3D高斯场（可渲染任意时刻的新视角）                │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.2 第一阶段：全局外观更新（Global Appearance Update）

**目标**：学习全局光照变化，仅优化4D哈希网格。

**迭代次数**：7,000 次

#### 3.2.1 全局外观模型（Global Appearance Model）

使用 **4D哈希网格 + Tiny MLP** 作为全局外观模型，推断不同时刻高斯基元上的增量缩放和球谐属性：

$$\{\Delta s^t_i, \Delta Y^t_i\} = \mathcal{F}(\mathcal{H}\{\mu_i, t\})$$

其中：
- $\mathcal{H}$：4D哈希网格（输入为3D位置 $\mu_i$ 和时间 $t$）
- $\mathcal{F}$：Tiny MLP解码器
- $\Delta s^t_i \in \mathbb{R}^3$：增量缩放
- $\Delta Y^t_i \in \mathbb{R}^{48}$：增量球谐系数

#### 3.2.2 外观模型网络详细结构

| 组件 | 详细参数 |
|------|---------|
| **4D哈希网格** | |
| 哈希表层数 L | 16 层 |
| 每层特征维度 F | 4 |
| 最粗网格分辨率 | 16 |
| 输入维度 | 4（3D位置 + 1D时间戳） |
| **Tiny MLP** | |
| 网络层数 | **2 层**全连接 |
| 每层神经元数 | **128** |
| 输出维度 | **51**（48维球谐系数 + 3维缩放） |

#### 3.2.3 布局不变区域掩码（Layout-invariant Mask）

为避免新增/消失物体的影响，利用 **SAM（Segment Anything Model）** 进行实例分割：

1. 对新采集图像 $I^t_f$ 和用旧模型渲染的图像 $I^{t-1}_f$，使用SAM获取实例分割掩码 $\{S^t_f, S^{t-1}_f\}$
2. 计算对应实例的 **IoU** 分数
3. 在两个时间步IoU分数都高的实例被掩码为 $\{M^{t-1}_f, M^t_f\}$
4. 最终布局不变掩码来自 $M^{t-1}_f$ 和 $M^t_f$ 的交集

#### 3.2.4 损失函数

使用掩码加权的光度损失：

$$\mathcal{L}_{st} = (1 - \lambda)\mathcal{L}_1 \cdot M_f + \lambda\mathcal{L}_{D-SSIM} \cdot M_f$$

其中：

$$\mathcal{L}_1 = \frac{1}{HW}\sum_{n=1}^{HW}|\hat{C}_n - C_n|$$

$$\mathcal{L}_{D-SSIM} = \frac{1}{HW}\text{SSIM}(\hat{C}, C)$$

**训练设置**：
- densify操作**关闭**
- 仅优化4D哈希网格
- 使用布局不变掩码引导优化

---

### 3.3 第二阶段：几何布局更新（Geometric Layout Update）

**目标**：处理几何布局变化（物体新增和消失）。

**迭代次数**：8,000 次

**关键约束**：第一阶段学到的全局外观模型**保持固定**。

#### 3.3.1 新增物体处理（Addition）

对于新出现的物体：
1. 使用 **COLMAP（SFM）** 从当前时刻图像中提取稀疏点云
2. 将稀疏点作为新3D高斯基元 $G_{add}$ 添加到现有高斯场
3. **开启 densify 操作**，仅对新增点进行致密化

#### 3.3.2 消失物体处理（Removal）

对于消失的物体，引入**可学习移除因子** $m$：

$$\psi(m) = \frac{1}{1 + e^{-1000m}}$$

- $\psi(m)$ 仅乘以先前高斯 $G_p$ 的不透明度
- 使用正则化项驱动 $\psi(m)$ 趋向 0 或 1：

$$\mathcal{L}_{reg} = \lambda_2(1 - \psi(m))\psi(m) + \lambda_3 \text{BCE}(\psi(m), 1)$$

#### 3.3.3 损失函数

$$\mathcal{L}_{nd} = (1 - \lambda_1)\mathcal{L}_1 + \lambda_1\mathcal{L}_{D-SSIM} + \mathcal{L}_{reg}$$

#### 3.3.4 剪枝策略

1. **阈值剪枝**：设置阈值 $\tau = 0.01$，当 $\psi(m) < \tau$ 时，将对应高斯标记为待移除候选 $G_c$
2. **DBSCAN 过滤**：对 $G_c$ 使用 **DBSCAN聚类算法** 过滤远离消失物体表面的稀疏离群点
3. **包围盒剪枝**：剩余高斯被聚类到一个包围盒中，该包围盒内的高斯从 $G_p$ 中剪枝
4. **剪枝时机**：在第二阶段训练到 **5K迭代** 和 **8K迭代** 时分别执行DBSCAN过滤

#### 3.3.5 可见性池管理

被剪枝的高斯**不会真正删除**，而是：
- 在可见性池中被设为**不活跃（inactive）**
- 反之，新增高斯设为**活跃（active）**
- 这使得方法能够实现**基元级别的场景控制**和**场景变化可视化**

---

### 3.4 第三阶段：联合精炼（Joint Refinement）

**目标**：消除剪枝后渲染伪影，联合优化外观模型和新增高斯属性。

**迭代次数**：15,000 次

#### 3.4.1 损失函数

$$\mathcal{L}_{rd} = (1 - \lambda)\mathcal{L}_1 + \lambda\mathcal{L}_{D-SSIM}$$

同时优化：
- 外观模型（4D Hash Grid）
- 新增高斯基元 $G_{add}$ 的属性

#### 3.4.2 重要性剪枝（Importance Pruning）

在第三阶段训练到 **4K迭代** 时执行：

$$v_i = \max_{n \in \mathcal{N}_1}(\alpha_{n,i} T_{n,i})$$

其中 $\mathcal{N}_1$ 是第 $i$ 个高斯在所有训练视角中关联的像素集合。重要性分数低于 **0.05** 的高斯被丢弃。

---

## 四、可见性感知的持续学习

### 4.1 可见性池（Visibility Pool）

为每个高斯在整个时间段内维护一个可见性池 $\mathcal{P}_v$：

| 功能 | 描述 |
|------|------|
| 活跃高斯 | 当前时刻可见的高斯基元 |
| 不活跃高斯 | 被剪枝的高斯（物体消失），保留在池中 |
| 历史相机外参 | 存储所有历史训练视角的相机外参 |

### 4.2 生成式回放（Generative Replay）

利用3D高斯场的高效渲染能力进行生成式回放：

1. 在每个新时间步，基于**先前相机位姿**和已训练的3D高斯场
2. **重新渲染**过去的视角作为历史线索
3. 将渲染的历史图像加入训练数据集
4. **无需存储原始历史图像**，仅需保存少量相机外参

**优势**：
- 存储开销低（仅需相机外参）
- 利用3DGS的实时渲染能力
- 有效缓解灾难性遗忘

---

## 五、训练流程总览

### 5.1 完整训练流程

```
时间步 t 的新数据到达
        │
        ▼
┌─────────────────────────────────────┐
│  预处理                              │
│  1. COLMAP/SFM 提取稀疏点云           │
│  2. SAM 实例分割 → 布局不变掩码        │
│  3. 用旧模型渲染历史视角（生成式回放）   │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  第一阶段：全局外观更新（7K 迭代）      │
│  - 优化：4D Hash Grid                │
│  - 固定：所有高斯属性                  │
│  - densify：关闭                      │
│  - 损失：掩码加权的 L1 + D-SSIM       │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  第二阶段：几何布局更新（8K 迭代）      │
│  - 优化：移除因子 m + G_add 属性       │
│  - 固定：4D Hash Grid（外观模型）      │
│  - densify：开启（仅对新增点）          │
│  - 5K/8K迭代时执行DBSCAN剪枝          │
│  - 损失：L1 + D-SSIM + 正则化         │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  第三阶段：联合精炼（15K 迭代）         │
│  - 优化：4D Hash Grid + G_add 属性     │
│  - 4K迭代时执行重要性剪枝              │
│  - 损失：L1 + D-SSIM                 │
└─────────────────────────────────────┘
        │
        ▼
  更新后的3D高斯场
  （可渲染任意时刻的新视角）
```

### 5.2 训练超参数汇总

| 参数 | 值 |
|------|-----|
| **第一阶段迭代数** | 7,000 |
| **第二阶段迭代数** | 8,000 |
| **第三阶段迭代数** | 15,000 |
| **总迭代数** | 30,000 |
| **移除阈值 τ** | 0.01 |
| **重要性剪枝阈值** | 0.05 |
| **DBSCAN执行时机** | 第2阶段 5K、8K 迭代 |
| **重要性剪枝时机** | 第3阶段 4K 迭代 |
| **时间步归一化** | 归一化到 [0, 1] |
| **GPU** | NVIDIA RTX 3090 (24GB) |

### 5.3 网络架构详细参数

| 组件 | 参数 |
|------|------|
| **4D Hash Grid** | |
| 哈希表层数 | 16 |
| 每层特征维度 | 4 |
| 最粗网格分辨率 | 16 |
| 总参数量（哈希表） | T = 2^21 = 2,097,152 个条目 |
| **Tiny MLP** | |
| 层数 | 2 层全连接 |
| 隐藏层宽度 | 128 |
| 输出维度 | 51（48维SH + 3维scale） |
| **3D高斯初始化** | |
| 初始方法 | MVS 密集点云 |
| 天空场景 | Sky Ball（100,000个均匀分布点，半径为初始点云10倍） |

---

## 六、Sky Ball 机制

针对 WAT 数据集中大量无界天空区域：

1. 确定MVS初始点云的中心和半径
2. 在一个3D球体内均匀分布 **100,000 个点**
3. 球体半径为初始点云半径的 **10 倍**
4. 这些点作为天空区域的初始化高斯

---

## 七、实验结果

### 7.1 数据集

| 数据集 | 场景数 | 描述 |
|--------|--------|------|
| **WAT** | 10个真实场景 | 室内+室外，随时间显著变化 |
| **Synthetic NeRF** | 8个场景 | 静态场景，训练集分为10个序列子集 |
| **Tanks&Temples** | 2个场景 | 额外验证 |

### 7.2 主要性能对比

**WAT 数据集平均结果**：

| 方法 | PSNR (↑) | SSIM (↑) | 渲染速度 |
|------|----------|----------|---------|
| Baseline | 16.89 | 0.631 | - |
| CLNeRF | 25.45 | 0.764 | 0.75 FPS |
| 4DGS | 25.70 | 0.816 | - |
| **GaussianUpdate** | **26.31** | **0.830** | **34 FPS** |
| UB（上界） | 26.47 | 0.830 | - |

**Synthetic NeRF 数据集平均结果**：

| 方法 | PSNR (↑) | SSIM (↑) |
|------|----------|----------|
| Baseline | 22.27 | 0.812 |
| CLNeRF | 32.16 | 0.957 |
| 4DGS | 32.18 | 0.959 |
| **GaussianUpdate** | **33.44** | **0.967** |
| UB（上界） | 33.87 | 0.969 |

### 7.3 消融实验

| 配置 | PSNR (↑) | SSIM (↑) |
|------|----------|----------|
| w/o 移除因子 | 25.87 | 0.834 |
| w/o 新增高斯 | 25.96 | 0.829 |
| w/o 布局不变掩码 | 26.18 | 0.839 |
| **完整模型** | **26.55** | **0.844** |

### 7.4 内存效率

| 指标 | GaussianUpdate | 每时刻独立训练 |
|------|---------------|--------------|
| 平均高斯点数 | 870,000 | 1,500,000 |
| 减少比例 | - | **减少42%** |

---

## 八、方法优势与局限

### 8.1 优势

1. **实时渲染**：34 FPS（vs CLNeRF 的 0.75 FPS）
2. **显式变化建模**：可检测和可视化场景几何变化
3. **无需存储历史图像**：仅需保存相机外参
4. **内存高效**：比独立训练每个时刻减少42%的高斯点数
5. **渐进式解耦**：三阶段策略有效分离光照和几何变化

### 8.2 局限性

- 无法准确建模强反射场景（如镜子）
- 反射区域的更新可能导致不正确的几何或外观

---

## 九、关键公式汇总

| 公式 | 描述 |
|------|------|
| $\mathcal{G} = \{G_i : (\mu_i, \Sigma_i, \sigma_i, Y_i)\}$ | 3D高斯基元集合 |
| $\Sigma = RSS^TR^T$ | 协方差矩阵参数化 |
| $\Sigma' = JW\Sigma W^TJ^T$ | 3D→2D投影 |
| $\mathcal{C} = \sum c_i \alpha_i T_i$ | α-blending渲染 |
| $\{\Delta s^t_i, \Delta Y^t_i\} = \mathcal{F}(\mathcal{H}\{\mu_i, t\})$ | 全局外观模型推理 |
| $\psi(m) = \frac{1}{1+e^{-1000m}}$ | 移除因子激活函数 |
| $\mathcal{L}_{reg} = \lambda_2(1-\psi)\psi + \lambda_3\text{BCE}(\psi,1)$ | 移除正则化 |
| $v_i = \max(\alpha_{n,i} T_{n,i})$ | 重要性分数 |
